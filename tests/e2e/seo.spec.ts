import { test, expect } from '@playwright/test';import { publicRoutes,campaignRoutes } from './helpers';
test('indexable pages have metadata and sitemap behavior is correct',async({page,request})=>{const titles=new Set<string>();for(const route of publicRoutes){await page.goto(route);const title=await page.title();expect(title.length).toBeGreaterThan(5);expect(titles.has(title)).toBeFalsy();titles.add(title);await expect(page.locator('link[rel="canonical"]')).toHaveCount(1);await expect(page.locator('meta[name="robots"]')).not.toHaveAttribute('content',/noindex/);}const site=await (await request.get('/sitemap.xml')).text();expect(site).not.toMatch(/\/campaigns\//);expect(site).not.toMatch(/\/api\//);const robots=await (await request.get('/robots.txt')).text();expect(robots).toMatch(/Disallow: \/api\//i);expect(robots).toMatch(/Sitemap:/i);});
test('campaigns are noindex and self canonical',async({page})=>{for(const route of campaignRoutes){await page.goto(route);await expect(page.locator('meta[name="robots"]')).toHaveAttribute('content',/noindex/);await expect(page.locator('link[rel="canonical"]')).toHaveAttribute('href',new RegExp(`${route}$`));}});
test('structured data parses and avoids fake local proof',async({page})=>{await page.goto('/');const blocks=await page.locator('script[type="application/ld+json"]').allTextContents();for(const b of blocks){const data=JSON.parse(b);expect(JSON.stringify(data)).not.toMatch(/LocalBusiness|aggregateRating|review|telephone|sameAs|address/i);}});
