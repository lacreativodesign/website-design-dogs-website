import { test, expect } from '@playwright/test';
test.use({viewport:{width:390,height:844}});test('mobile menu opens, closes, and supports links',async({page})=>{await page.goto('/');const opener=page.getByRole('button',{name:/menu|navigation/i});await expect(opener).toBeVisible();await opener.click();await expect(page.getByRole('dialog').or(page.getByRole('navigation',{name:/mobile/i}))).toBeVisible();expect(await page.evaluate(()=>getComputedStyle(document.body).overflow)).toMatch(/hidden|clip/);await page.keyboard.press('Escape');await expect(opener).toBeFocused();await opener.click();await page.getByRole('link',{name:/contact/i}).first().click();await expect(page).toHaveURL(/contact/);});
