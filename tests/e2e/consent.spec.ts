import { test, expect } from '@playwright/test';
test('consent banner rejects, accepts, customizes, and reopens',async({page})=>{await page.goto('/');await expect(page.getByText(/cookie|consent/i).first()).toBeVisible();await expect(page.getByRole('button',{name:/accept all/i})).toBeVisible();await expect(page.getByRole('button',{name:/reject optional/i})).toBeVisible();await page.getByRole('button',{name:/reject optional/i}).click();expect(await page.evaluate(()=>document.cookie)).toContain('wdd-consent');expect(await page.evaluate(()=>window.dataLayer||[])).toEqual(expect.not.arrayContaining([expect.objectContaining({event:'gtm.js'})]));await page.getByRole('button',{name:/cookie settings/i}).click();await expect(page.getByRole('dialog',{name:/cookie settings/i})).toBeVisible();await page.keyboard.press('Escape');await expect(page.getByRole('dialog',{name:/cookie settings/i})).toHaveCount(0);});
