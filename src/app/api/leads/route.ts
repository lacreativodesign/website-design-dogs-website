import { NextResponse } from "next/server"; import { MAX_BODY_BYTES } from "@/lib/leads/constants"; import { sendToBizosto } from "@/lib/leads/bizosto-adapter"; import { LeadError, errorResponse } from "@/lib/leads/errors"; import { logLead, logLeadError } from "@/lib/leads/logger"; import { assertCanSubmit, assertOrigin, clientId, getLeadConfig } from "@/lib/leads/request-security"; import { checkRateLimit } from "@/lib/leads/rate-limit"; import { verifyTurnstile } from "@/lib/leads/turnstile"; import { validateLeadPayload } from "@/lib/leads/validation"; import type { ClientLeadPayload, LeadSubmissionResponse } from "@/lib/leads/types";
const headers={"Cache-Control":"no-store","X-Content-Type-Options":"nosniff"};
function json(body:LeadSubmissionResponse,status:number,extra?:HeadersInit){return NextResponse.json(body,{status,headers:{...headers,...extra}})}
export async function POST(req:Request){const started=Date.now(); const requestId=crypto.randomUUID(); let payload:ClientLeadPayload|undefined; try{const cfg=getLeadConfig(); if(!req.headers.get("content-type")?.toLowerCase().includes("application/json")) throw new LeadError("INVALID_REQUEST","Unsupported content type.",415); const text=await req.text(); if(new TextEncoder().encode(text).byteLength>MAX_BODY_BYTES) throw new LeadError("INVALID_REQUEST","Request body is too large.",413); try{payload=JSON.parse(text) as ClientLeadPayload}catch{throw new LeadError("INVALID_REQUEST","Invalid JSON request.",400)} assertOrigin(req,cfg); assertCanSubmit(cfg); checkRateLimit(clientId(req),cfg.rateWindowMs,cfg.rateMax); const envelope=validateLeadPayload(payload); await verifyTurnstile(payload.turnstileToken,clientId(req),cfg); const result=await sendToBizosto(envelope,cfg); logLead({requestId,submissionId:envelope.submissionId,formType:envelope.formType,stage:"bizosto",status:"success",durationMs:Date.now()-started,upstreamStatus:result.upstreamStatus}); return json({ok:true,message:"Thanks — your request was received successfully. We’ll review the details and respond using the contact information you provided.",...(result.referenceId?{referenceId:result.referenceId}:{})},201)}catch(e){const err=e instanceof LeadError?e:new LeadError("INTERNAL_ERROR","We couldn’t send your request right now. Your information is still in the form, so you can try again.",500); const meta={requestId,submissionId:payload?.submissionId,formType:payload?.formType,stage:"api",status:"failed",durationMs:Date.now()-started,failure:err.code}; if(err.status>=500) logLeadError(meta); else logLead(meta); const extra=err.retryAfterSeconds?{"Retry-After":String(err.retryAfterSeconds)}:undefined; return json(errorResponse(err),err.status,extra)}}
