import type { Metadata } from "next";import Image from "next/image";import Link from "next/link";import { pageMetadata } from "@/lib/seo";import { portfolioConcepts, portfolioFilters } from "@/content/portfolio";import { ScenicHero, MockPage } from "@/components/mockup/mockup-components";
export const metadata: Metadata = pageMetadata({ title:"Portfolio Concepts", description:"Website design concepts for growing businesses.", path:"/portfolio" });
export default function Page(){return <MockPage><ScenicHero title="Portfolio" current="Portfolio" body="A collection of website concepts designed for growing businesses." dark="/brand/scenes/portfolio-hero-dark.webp" light="/brand/scenes/portfolio-hero-light.webp"/><section className="mock-section"><div className="mock-container"><div role="tablist" aria-label="Portfolio filters" className="mock-actions">{portfolioFilters.map((f,i)=><button role="tab" aria-selected={i===0} className={i===0?"mock-btn":"mock-btn mock-btn--ghost"} key={f}>{f}</button>)}</div><div className="mock-grid mock-grid--4" style={{marginTop:24}}>{portfolioConcepts.map(p=><article data-testid="portfolio-concept-card" className="mock-card" key={p.title}><div className="portfolio-img"><Image src={p.image} alt="" fill sizes="25vw"/></div><p className="mock-link">DESIGN CONCEPT</p><h3>{p.title}</h3><p>{p.category}</p></article>)}</div><p style={{textAlign:"center",marginTop:28}}><Link className="mock-btn" href="/get-started">View All Projects</Link></p></div></section></MockPage>}
