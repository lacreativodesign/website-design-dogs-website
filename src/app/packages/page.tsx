import type { Metadata } from "next";import Link from "next/link";import { pageMetadata } from "@/lib/seo";import { websitePackages } from "@/content/packages";import { ScenicHero, MockPage } from "@/components/mockup/mockup-components";
export const metadata: Metadata = pageMetadata({ title:"Website Packages", description:"Simple, transparent website pricing.", path:"/packages" });
export default function Page(){return <MockPage><ScenicHero title="Packages" current="Packages" body="Simple, transparent pricing that fits your business." dark="/brand/scenes/packages-hero-dark.webp" light="/brand/scenes/packages-hero-light.webp"/><section className="mock-section"><div className="mock-container"><div className="mock-actions" role="tablist" aria-label="Package selector"><button className="mock-btn" role="tab" aria-selected>Website Packages</button><button className="mock-btn mock-btn--ghost" role="tab" aria-selected={false}>Website Care</button></div><div className="mock-grid mock-grid--3" style={{marginTop:36}}>{websitePackages.map(p=><article data-testid="pricing-card" className={`mock-card ${p.label?"recommended":""}`} key={p.slug}>{p.label?<p className="mock-link">RECOMMENDED</p>:null}<h3>{p.name}</h3><p className="price">{p.price}</p><p>{p.description}</p><ul className="mock-list">{p.features.slice(0,5).map(f=><li key={f}>{f}</li>)}</ul><Link className="mock-btn" style={{marginTop:18}} href={p.href}>{p.cta}</Link></article>)}</div><div className="mock-grid mock-grid--3" style={{marginTop:28}}>{["Clear Project Scope","Third-Party Costs Confirmed","Launch Support Included as Scoped"].map(v=><div className="mock-card" key={v}><h3>{v}</h3></div>)}</div><p style={{textAlign:"center",marginTop:28}}><Link className="mock-btn" href="/get-started">View All Packages</Link></p></div></section></MockPage>}
