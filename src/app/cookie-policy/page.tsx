import { Container } from "@/components/ui/container";
import { Section } from "@/components/ui/section";

export default function Page() {
  return <Section><Container className="grid gap-6"><h1 className="text-4xl font-black">Cookie Policy</h1><p>Website Design Dogs is a service brand of LA CREATIVO GROUP, LLC.</p><p>The lead forms may use first-party session storage during the current browser session to preserve campaign attribution such as landing page, referrer, selected UTM fields, fbclid, and gclid. This is not a long-term tracking cookie and is submitted only when you send a form.</p><p>No advertising cookie banner is added by this policy update.</p></Container></Section>;
}
