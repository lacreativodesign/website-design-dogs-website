import { CONSENT_COOKIE_NAME, CONSENT_VERSION, type ConsentPreferences } from "./consent-types";
export function makeConsent(analytics:boolean, marketing:boolean):ConsentPreferences { return { version: CONSENT_VERSION, necessary: true, analytics, marketing, updatedAt: new Date().toISOString() }; }
export function parseConsent(value:string | null | undefined):ConsentPreferences | null { if(!value) return null; try{ const p=JSON.parse(decodeURIComponent(value)) as Partial<ConsentPreferences>; if(p.version!==CONSENT_VERSION || p.necessary!==true || typeof p.analytics!=="boolean" || typeof p.marketing!=="boolean") return null; return {version:CONSENT_VERSION,necessary:true,analytics:p.analytics,marketing:p.marketing,updatedAt:typeof p.updatedAt==="string"?p.updatedAt:""}; }catch{return null;} }
export function readConsent():ConsentPreferences | null { if(typeof document==="undefined") return null; const part=document.cookie.split("; ").find((row)=>row.startsWith(`${CONSENT_COOKIE_NAME}=`)); return parseConsent(part?.split("=").slice(1).join("=")); }
export function writeConsent(prefs:ConsentPreferences){ const secure=location.protocol==="https:"?"; Secure":""; document.cookie=`${CONSENT_COOKIE_NAME}=${encodeURIComponent(JSON.stringify(prefs))}; Path=/; SameSite=Lax; Max-Age=${60*60*24*180}${secure}`; }
