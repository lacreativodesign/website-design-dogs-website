"use client";
import Script from "next/script"; import { useEffect } from "react"; import { getGtmId } from "@/lib/site-config"; import type { ConsentPreferences } from "./consent-types"; import { defaultConsent, ensureDataLayer, updateConsent } from "./consent-mode";
export function TrackingLoader({ prefs }: { prefs: ConsentPreferences | null }){ const id=getGtmId(); const load=Boolean(id && prefs && (prefs.analytics||prefs.marketing)); useEffect(()=>{ if(!id)return; defaultConsent(); if(prefs) updateConsent(prefs); },[id,prefs]); if(!id || !load) return null; return <Script id="wdd-gtm" strategy="afterInteractive" src={`https://www.googletagmanager.com/gtm.js?id=${id}`} onLoad={()=>{ensureDataLayer(); window.__wddGtmLoaded=true; window.dataLayer?.push({event:"gtm.js", "gtm.start": Date.now()}); if(prefs) updateConsent(prefs);}} />; }
