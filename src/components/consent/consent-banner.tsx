"use client"; import { useEffect } from "react"; import { trackEvent } from "@/lib/tracking/events";
export function ConsentBanner({onAccept,onReject,onCustomize}:{onAccept:()=>void;onReject:()=>void;onCustomize:()=>void}){ useEffect(()=>trackEvent("wdd_consent_banner_viewed",{}),[]); return <section role="region" aria-label="Cookie consent" className="fixed inset-x-4 bottom-4 z-50 mx-auto max-w-4xl rounded-3xl border border-[var(--color-border)] bg-[var(--color-surface)] p-5 shadow-2xl"><div className="grid gap-4 md:grid-cols-[1fr_auto]"><div><h2 className="text-lg font-black">Cookie preferences</h2><p className="mt-2 text-sm leading-6 text-[var(--color-text-muted)]">We use necessary storage for site operation. Optional analytics and marketing tags load only after you consent.</p></div><div className="flex flex-wrap items-center gap-3"><button className="button button-outline" onClick={onReject}>Reject Optional</button><button className="button button-outline" onClick={onCustomize}>Customize</button><button className="button button-primary" onClick={onAccept}>Accept All</button></div></div></section>; }
