"use client"; import { useEffect, useRef } from "react"; import { usePathname } from "next/navigation"; import { trackEvent } from "@/lib/tracking/events"; import { readConsent } from "./consent-storage";
function pageType(path:string){ if(path.startsWith("/campaigns/")) return "campaign"; if(path==="/") return "home"; return path.split("/")[1]||"page"; }
export function PageViewTracker(){ const pathname=usePathname(); const last=useRef(""); useEffect(()=>{ const view=()=>{ const prefs=readConsent(); if(!prefs?.analytics || process.env.NEXT_PUBLIC_ENABLE_GTM!=="true") return; if(last.current===pathname) return; last.current=pathname; if(pathname.startsWith("/campaigns/")){ trackEvent("wdd_page_view",{pagePath:pathname,pageType:"campaign",campaignSlug:pathname.split("/")[2]}); } else { trackEvent("wdd_page_view",{pagePath:pathname,pageType:pageType(pathname)}); } }; view(); window.addEventListener("wdd:consent-updated",view); return()=>window.removeEventListener("wdd:consent-updated",view); },[pathname]); return null; }
